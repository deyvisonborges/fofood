import { Typography } from "@/components/typography";
import { HeroSection } from "./components/hero-section";

export function ViewHome() {
  return (
    <>
      <HeroSection />
      <Typography variant="heading-1">Meu oivo</Typography>
      <Typography variant="heading-1">Meu oivo</Typography>
      <Typography variant="heading-1">Meu oivo</Typography>
      <Typography variant="heading-1">Meu oivo</Typography>
    </>
  );
}
